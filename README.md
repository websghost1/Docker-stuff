# Docker stuff
Repo for docker and docker (docker compose too) scripts

This script will backup your container with "commit" and "save" will create a new container from the one that you just backed,
 and will clean backup directories that are older than last 7 days.


